# Server-side only — used by Next.js rewrites to forward /api/* requests to the
# backend through the load-balancer.  This variable has no NEXT_PUBLIC_ prefix
# so it is never included in the browser bundle; clients never learn the backend
# address and can only reach it through the proxy.
BACKEND_URL=http://{{ lb_virtual_ip }}
