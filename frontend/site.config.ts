/**
 * Site branding configuration.
 * Edit this file to customise the site name, tagline, accent colour, and logo.
 */
const siteConfig = {
  siteName: 'Lethe',
  siteTagline: 'Self-hosted data archival service',
  /** Primary accent colour used for interactive elements (hex string). */
  accentColor: '#6366f1',
  /** Optional URL or path for a logo image. Set to null to use the site name text. */
  logoUrl: null as string | null,
};

export default siteConfig;
